import React, {Component} from 'react';

export default class Loading extends Component {
   
    render() {
        console.log(1);
        return (
            <div>page loading...</div>
        )
    }
}